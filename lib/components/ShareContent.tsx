'use client'

// step 1: render component with static data
// step 2: move state to this file
// step 3: move buttons for state toggles to this component
// step 4: use the dynamic data to render the components

export const ShareContent = () => {

    // STATE HERE

  // const filterData = () =>
  //   data.filter((d) => {
  //     return active ? d.isActive : true
  //   })

  // const toggleShare = () => {
  //   setState((prev) => ({ ...prev, showShare: !prev.showShare }))
  // }

  // const toggleActive = () => {
  //   setState((prev) => ({ ...prev, active: !prev.active }))
  // }

  // const filteredData = filterData()

    return(
        <>
        {/* Donut */}
        {/* <Donut data={fileteredData}/> */}
        {/* table */}
        {/* <Table data={fileteredData} /> */}
        </>
    )
}